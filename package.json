{
  "name": "flowsync-consulting-website",
  "version": "1.0.0",
  "description": "FlowSync Consulting - NetSuite workflow automation and integration",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "./tailwindcss -i src/css/input.css -o _site/css/main.css --minify && npx @11ty/eleventy",
    "build:eleventy": "npx @11ty/eleventy",
    "build:prod": "./tailwindcss -i src/css/input.css -o _site/css/main.css --minify && npx @11ty/eleventy"
  },
  "keywords": ["netsuite", "consulting", "workflow", "automation"],
  "author": "Ben Saralegui",
  "license": "UNLICENSED",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
